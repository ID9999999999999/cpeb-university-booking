{
  "name": "cpeb-admin-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check api.js && node --check app.js",
    "test": "node --test tests/*.test.mjs",
    "ci": "npm run check && npm test"
  }
}
